import logging
from zope.interface import Interface
import interfaces
from plone.indexer.decorator import indexer
LOG = logging.getLogger(__name__)

@indexer(interfaces.IOdooProduct)
def get_price(object):
    return getattr(object, 'price', None)

